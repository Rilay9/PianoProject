import { renderPlaceholderScreen } from './placeholder';

export function LibraryScreen(): HTMLElement {
  return renderPlaceholderScreen(
    'Library',
    'Search and filter every song, exercise, and drill, and import your own MusicXML files.',
  );
}

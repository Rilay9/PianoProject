import { renderPlaceholderScreen } from './placeholder';

export function PlanScreen(): HTMLElement {
  return renderPlaceholderScreen(
    'Plan',
    'Browse stages, units, and lessons here, with exercise and song options for each.',
  );
}

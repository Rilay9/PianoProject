# Common header (prepended to every prompt — copy it in if you paste a prompt alone)

You are building **PianoPath**, a piano-teaching PWA for an Android phone, in the repository
you are checked out in. Before doing anything else, read `docs/00-overview.md` fully, then the
docs named in the prompt below. The docs are normative; if you must deviate, write a short
note in `docs/decisions/<date>-<topic>.md` explaining why, and continue.

Rules for this session:
1. Work on the branch named in the prompt (create from `main` if missing). Commit early and
   often with conventional-commit messages. Never mention AI model names in commits or code.
2. Everything you build must be verified by a command you actually ran: unit tests, e2e tests,
   a build, a validator. Paste the exact commands and their results in your final report.
   Do not claim something works because it should.
3. Keep the scope of the prompt. If you notice work outside it, list it under "Follow-ups"
   in the report instead of doing it.
4. When a decision has no default in the docs and different choices lead to materially
   different work, pick the simpler option, state it explicitly, and continue.
5. Final report format: **Done** (bullets, with the verification command for each) · **Not done
   / blocked** (why) · **Follow-ups** · **Questions for the owner** · **Files touched** (paths).
